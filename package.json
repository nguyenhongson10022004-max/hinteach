{
    "name": "hinteach",
    "version": "0.1.0",
    "private": true,
    "description": "HinTeach WordPress Plugin — Build tools",
    "scripts": {
        "build": "node build.mjs",
        "watch": "node build.mjs --watch"
    },
    "devDependencies": {
        "esbuild": "^0.21.0"
    }
}
